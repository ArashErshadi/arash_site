---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import StoryBlockRenderer from '../../../components/StoryBlockRenderer.astro';
import Lightbox from '../../../components/Lightbox.astro';
import ProjectCard from '../../../components/ProjectCard.astro';
import { getProjects, getProjectBySlug, getAdjacentProjects, getRelatedProjects } from '../../../utils/projects';

export async function getStaticPaths() {
  const projects = await getProjects('fa');
  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
}

const { project } = Astro.props;
const { prev, next } = await getAdjacentProjects(project.slug, 'fa');
const relatedProjects = await getRelatedProjects(project.slug, 'fa', 2);

const structuredData = [
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    'itemListElement': [
      {
        '@type': 'ListItem',
        'position': 1,
        'name': 'خانه',
        'item': 'https://mg.github.io/fa/'
      },
      {
        '@type': 'ListItem',
        'position': 2,
        'name': 'پروژه‌ها',
        'item': 'https://mg.github.io/fa/projects/'
      },
      {
        '@type': 'ListItem',
        'position': 3,
        'name': project.content.title,
        'item': `https://mg.github.io/fa/projects/${project.slug}/`
      }
    ]
  },
  {
    '@context': 'https://schema.org',
    '@type': 'CreativeWork',
    'name': project.content.title,
    'headline': project.content.excerpt,
    'description': project.content.premise,
    'dateCreated': String(project.year),
    'author': {
      '@type': 'Person',
      'name': 'آرش ارشادی'
    },
    'image': project.cover
  }
];
---

<BaseLayout
  title={`${project.content.title} | آرش ارشادی`}
  description={project.content.excerpt}
  lang="fa"
  currentPath={`/fa/projects/${project.slug}/`}
  ogImage={project.cover}
  structuredData={structuredData}
>
  <article class="project-detail-article">
    <!-- Breadcrumb Navigation -->
    <nav class="site-container breadcrumb-nav" aria-label="مسیر راهنما">
      <ol class="breadcrumb-list text-xs text-muted">
        <li><a href="/fa/">صفحه اصلی</a></li>
        <li aria-hidden="true">/</li>
        <li><a href="/fa/projects/">پروژه‌ها</a></li>
        <li aria-hidden="true">/</li>
        <li aria-current="page" class="text-text">{project.content.title}</li>
      </ol>
    </nav>

    <!-- Project Header & Premise -->
    <header class="site-container project-header">
      <div class="project-header-grid site-grid">
        <div class="project-title-col">
          <span class="project-order-tag text-mono text-label">{String(project.order).padStart(2, '0')} // سند فنی معماری</span>
          <h1 class="project-headline">{project.content.title}</h1>
          <p class="project-lead text-muted">{project.content.excerpt}</p>
        </div>

        <!-- Metadata Plate Sidebar -->
        <aside class="project-meta-plate">
          <div class="meta-plate-header hairline-border-b">
            <span class="text-label">مشخصات فنی // سال {project.year}</span>
          </div>
          <dl class="meta-plate-list">
            <div class="meta-item hairline-border-b">
              <dt class="text-label">موقعیت بستر</dt>
              <dd>{project.location}</dd>
            </div>
            <div class="meta-item hairline-border-b">
              <dt class="text-label">گونه‌شناسی</dt>
              <dd>{project.typology}</dd>
            </div>
            <div class="meta-item hairline-border-b">
              <dt class="text-label">زیربنا / مقیاس</dt>
              <dd class="text-mono">{project.scale}</dd>
            </div>
            <div class="meta-item hairline-border-b">
              <dt class="text-label">وضعیت طرح</dt>
              <dd>{project.status}</dd>
            </div>
            <div class="meta-item hairline-border-b">
              <dt class="text-label">ابزارها و نرم‌افزارها</dt>
              <dd>{project.tools.join('، ')}</dd>
            </div>
            <div class="meta-item">
              <dt class="text-label">همکاران و استودیو</dt>
              <dd>{project.collaborators.join('، ')}</dd>
            </div>
          </dl>
        </aside>
      </div>
    </header>

    <!-- Full Width Hero Cover -->
    <div class="project-hero-media">
      <button
        type="button"
        class="lightbox-trigger"
        data-lightbox-src={project.cover}
        data-lightbox-caption={`${project.content.title} - نمای اصلی`}
        aria-label={`بزرگ‌نمایی تصویر اصلی ${project.content.title}`}
      >
        <img src={project.cover} alt={project.content.title} width="1600" height="1000" loading="eager" decoding="async" class="hero-cover-img" />
      </button>
    </div>

    <!-- Architectural Premise -->
    <section class="project-premise-section site-container-narrow">
      <span class="text-label">۰۱ // مبانی نظری و بستر طرح</span>
      <h2 class="premise-heading">رویکرد فضایی و سازماندهی محیطی</h2>
      <p class="premise-body">{project.content.premise}</p>
    </section>

    <!-- Story Blocks / Sections -->
    <StoryBlockRenderer blocks={project.content.storyBlocks} locale="fa" />

    <!-- Additional Gallery Plates -->
    {project.content.gallery.length > 0 && (
      <section class="project-gallery-section site-container">
        <div class="section-header hairline-border-b">
          <span class="text-label">۰۲ // نقشه‌های فنی و اسناد تصویری</span>
          <h2 class="section-title">مدارک و نقشه‌های تکمیلی</h2>
        </div>
        <div class="gallery-plates-grid">
          {project.content.gallery.map((img, i) => (
            <figure class="gallery-plate-item">
              <button
                type="button"
                class="lightbox-trigger"
                data-lightbox-src={img.src}
                data-lightbox-caption={img.caption || img.alt}
                aria-label={`بزرگ‌نمایی نقشه ${i + 1}: ${img.alt}`}
              >
                <img src={img.src} alt={img.alt} width="1600" height="1000" loading="lazy" decoding="async" />
              </button>
              {img.caption && (
                <figcaption class="text-xs text-muted">
                  <span class="caption-marker">{String(i + 1).padStart(2, '0')} //</span> {img.caption}
                </figcaption>
              )}
            </figure>
          ))}
        </div>
      </section>
    )}

    <!-- Adjacent Project Navigation -->
    <nav class="site-container project-pagination hairline-border-t hairline-border-b" aria-label="ناوبری پروژه‌ها">
      <div class="pagination-inner">
        {prev ? (
          <a href={`/fa/projects/${prev.slug}/`} class="pagination-link prev">
            <span class="text-label">← پروژه قبلی</span>
            <span class="pagination-title">{prev.content.title}</span>
          </a>
        ) : (
          <div class="pagination-placeholder"></div>
        )}

        <a href="/fa/projects/" class="pagination-archive-link text-label">
          [ بازگشت به آرشیو ]
        </a>

        {next ? (
          <a href={`/fa/projects/${next.slug}/`} class="pagination-link next">
            <span class="text-label">پروژه بعدی →</span>
            <span class="pagination-title">{next.content.title}</span>
          </a>
        ) : (
          <div class="pagination-placeholder"></div>
        )}
      </div>
    </nav>

    <!-- Related Projects -->
    {relatedProjects.length > 0 && (
      <section class="related-projects-section site-container">
        <div class="section-header">
          <span class="text-label">۰۳ // طرح‌های مرتبط</span>
          <h2 class="section-title">مطالعات هم‌راستا در گونه‌شناسی</h2>
        </div>
        <div class="related-projects-grid">
          {relatedProjects.map((p, idx) => (
            <ProjectCard project={p} locale="fa" index={idx} />
          ))}
        </div>
      </section>
    )}
  </article>

  <!-- Interactive Lightbox -->
  <Lightbox locale="fa" />
</BaseLayout>

<style>
  .breadcrumb-nav {
    padding-top: var(--space-6);
  }

  .breadcrumb-list {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    flex-wrap: wrap;
  }

  .project-header {
    padding-top: var(--space-8);
    padding-bottom: var(--space-12);
  }

  .project-title-col {
    grid-column: span 8;
  }

  .project-order-tag {
    display: block;
    margin-bottom: var(--space-3);
  }

  .project-headline {
    margin-bottom: var(--space-4);
    font-weight: 500;
  }

  .project-lead {
    font-size: var(--text-xl);
    line-height: var(--leading-relaxed);
  }

  .project-meta-plate {
    grid-column: span 4;
    background-color: var(--color-surface);
    border: 1px solid var(--color-border);
    align-self: start;
  }

  .meta-plate-header {
    padding: var(--space-3) var(--space-4);
    background-color: var(--color-bg-subtle);
  }

  .meta-plate-list {
    display: flex;
    flex-direction: column;
  }

  .meta-item {
    display: flex;
    flex-direction: column;
    padding: var(--space-3) var(--space-4);
    gap: 2px;
  }

  .meta-item dd {
    font-size: var(--text-sm);
    color: var(--color-text);
    font-weight: 500;
  }

  .project-hero-media {
    width: 100%;
    margin-bottom: var(--space-16);
  }

  .hero-cover-img {
    width: 100%;
    max-height: 75vh;
    object-fit: cover;
  }

  .project-premise-section {
    margin-bottom: var(--space-16);
  }

  .premise-heading {
    font-size: var(--text-3xl);
    margin-top: var(--space-2);
    margin-bottom: var(--space-6);
  }

  .premise-body {
    font-size: var(--text-lg);
    line-height: var(--leading-relaxed);
    color: var(--color-text-body);
  }

  .gallery-plates-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-8);
    margin-top: var(--space-8);
  }

  .gallery-plate-item {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .gallery-plate-item img {
    border: 1px solid var(--color-border);
  }

  .project-pagination {
    margin-block: var(--space-16);
    padding-block: var(--space-8);
  }

  .pagination-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-6);
    flex-wrap: wrap;
  }

  .pagination-link {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
    max-width: 320px;
    transition: color var(--transition-fast);
  }

  .pagination-link:hover {
    color: var(--color-accent);
  }

  .pagination-title {
    font-size: var(--text-base);
    font-weight: 500;
  }

  .related-projects-section {
    margin-bottom: var(--space-24);
  }

  .related-projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-6);
    margin-top: var(--space-8);
  }

  @media (max-width: 768px) {
    .project-title-col { grid-column: span 12; }
    .project-meta-plate { grid-column: span 12; }
    .gallery-plates-grid { grid-template-columns: 1fr; }
    .related-projects-grid { grid-template-columns: 1fr; }
  }
</style>
